node_modules/

# Secrets. `.env` alone does NOT match .env.local / .env.production, so match
# every variant and re-allow only the committed templates.
.env*
!.env.example

artifacts/
cache/
typechain-types/
.next/
out/
.superpowers/

# Deployment records are safe to commit (addresses only) — keep them tracked.
